apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "legalassist-ai.serviceAccountName" . }}
  labels:
    {{- include "legalassist-ai.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
